package project;

import java.awt.Graphics;
import java.awt.Image;
import javax.swing.ImageIcon;
import javax.swing.JPanel;

class LuxuryView$BackgroundPanel extends JPanel {
   ImageIcon bgIcon;
   Image bg;

   public LuxuryView$BackgroundPanel(LuxuryView var1, String bgPath) {
      this.this$0 = var1;
      this.bgIcon = new ImageIcon(bgPath);
      this.bg = this.bgIcon.getImage();
   }

   public void paintComponent(Graphics g) {
      g.drawImage(this.bg, 0, 0, this.getWidth(), this.getHeight(), this);
   }
}
